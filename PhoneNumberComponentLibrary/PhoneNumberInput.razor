<div class="input-group phone-input-group @CustomClass">
    @if (IncludeCountrySelect)
    {
        @if (IncludeContinentSelect)
        {
            <!-- Dropdown for selecting Continent -->
            <div class="dropdown">
                <button class="btn btn-outline-secondary dropdown-toggle @Border dropdown-fixed-btn continent-btn @CustomButtonClass"
                        type="button"
                        data-bs-toggle="dropdown"
                        aria-expanded="false">
                    @if (SelectedContinent != null)
                    {
                        @SelectedContinent.GetDescription()
                    }
                    else
                    {
                        <span>Select Continent</span>
                    }
                </button>
                <ul class="dropdown-menu dropdown-menu-scrollable">
                    @foreach (var continent in Enum.GetValues(typeof(ContinentEnum)).Cast<ContinentEnum>())
                    {
                        <li>
                            <a class="dropdown-item"
                               href="#" @onclick="() => OnContinentSelect(continent)">
                                @continent.GetDescription()
                            </a>
                        </li>
                    }
                </ul>
            </div>
        }

        <!-- Dropdown for selecting Country based on Continent -->
        <div class="dropdown">
            <button class="btn btn-outline-secondary dropdown-toggle @Border dropdown-fixed-btn country-btn @CustomButtonClass"
                    type="button"
                    data-bs-toggle="dropdown"
                    aria-expanded="false">
                @if (SelectedCountry != null)
                {
                    @if (ShowFlags && !string.IsNullOrEmpty(SelectedCountry.CountryIsoAlpha2))
                    {
                        <img src="@SelectedCountry.FlagPath(FlagFormat)" alt="" class="dropdown-flag" />
                    }
                    <span>@SelectedCountry.CountryName</span>
                }
                else
                {
                    <span>Select Country</span>
                }
            </button>
            <ul class="dropdown-menu dropdown-menu-scrollable">
                @foreach (var country in FilteredCountries)
                {
                    <li>
                        <a class="dropdown-item dropdown-item-custom"
                           href="#" @onclick="() => OnCountrySelect(country)">
                            @if (ShowFlags && !string.IsNullOrEmpty(country.CountryIsoAlpha2))
                            {
                                <img src="@country.FlagPath(FlagFormat)" alt="" class="dropdown-flag" />
                            }
                            <span>@country.CountryName (@country.CountryCallCode)</span>
                        </a>
                    </li>
                }
            </ul>
        </div>
    }

    <!-- Input for entering the phone number -->
    <input id="phone-number"
           type="tel"
           class="form-control @Border @CustomInputClass"
           placeholder="@(RequiredPlaceholder ? RequiredValueValidationMessage : Placeholder)"
           autocomplete="@AutoComplete"
           value="@PhoneNumber"
           required="@ShowStandardRequiredMessage"
           @onchange="OnPhoneNumberChange" />
</div>